# STATE: notebook-report
Updated: 2026-07-24 | Commit: d21f9f5 (M3 + M4 + M5, human-confirmed committed); M6 + M7 built this pass, not yet committed | Source: Claude Code session executing Module: Locked Sections + Polish

## Mission
Turn a messy Jupyter notebook into a locked, section-grouped stakeholder report with deterministic stale-output detection and human-confirmed (not autonomous) cell classification.
Out of scope: live re-execution, web UI, a classification benchmark, git-based staleness, multi-notebook reports, non-Python kernels, PDF export, auth/hosting (see PLAN.md Not building).

## Current status
Module 0 (Bootstrap) through M5 (LLM Cell Classifier + Gloss) are committed: 9228c41 (Module 0 + M1 + M2) and d21f9f5 (M3 + M4 + M5). M6 (Review Artifact) and M7 (Locked Sections + Polish) are built and passing this pass, neither committed yet — PLAN.md's final module. `git status --short` also shows `commit.html`, the throwaway manual-runbook file this session writes/overwrites each round — not part of any module's deliverable, safe to leave uncommitted or committed at the human's discretion.

M7 built directly on top of uncommitted M6, per explicit user instruction ("start module 7"), same pattern as M4-on-uncommitted-M3 (see Decisions, 2026-07-24). `notebook_report/render.py` adds `SECTION_ORDER = ("SETUP", "EXPLORATION", "RESULT", "ABANDONED", "UNCLASSIFIED")`, `SECTION_TITLES`, and `_group_cells(cells)`, which buckets cells by `proposed_label` (a cell with `proposed_label` not in the four real labels — i.e. `None` or the literal string `"UNCLASSIFIED"` — falls into the `UNCLASSIFIED` bucket) and drops empty buckets; `render_report` now passes `sections=_group_cells(cells)` to the template instead of a flat `cells` list. `templates/report.html.jinja` wraps each non-empty bucket in `<section class="label-group" data-label="{{ title }}">` with an `<h2>` heading, nesting the existing per-cell `<section class="cell">` markup (including the M6 `<span class="label">` and M4 gap-box) unchanged inside — the per-cell span was flagged in M6's Decisions as possibly redundant once grouping landed, but was kept as-is since M6's own acceptance test already asserts on it and PLAN.md's M7 scope is grouping, not span removal. `cli.py`'s `analyze` now wraps `parse_notebook` in `try/except (nbformat.reader.NotJSONError, nbformat.ValidationError)`, raising `click.ClickException` (clean message, exit 1, no raw traceback) — resolves CONTEXT.md trust boundary #1. Confirmed directly via a REPL check this pass that these are the two exceptions nbformat actually raises: `NotJSONError` (subclass of `ValueError`) on content that isn't JSON at all, `nbformat.ValidationError` (which is literally `jsonschema.exceptions.ValidationError`, re-exported) on JSON that fails the notebook schema.

Deviation flagged: grouping cells by canonical section order (rather than document order) changes the rendered order of `<span class="label">` elements, which broke M6's already-passing `test_analyze_writes_review_yaml_and_render_reflects_hand_edited_label` assertion (`labels == ["RESULT", "EXPLORATION"]` — document order — no longer holds once RESULT and EXPLORATION cells are grouped into separate, canonically-ordered sections). Updated the assertion to `["EXPLORATION", "RESULT"]` in the same pass, with an inline comment explaining why; the test still exercises the same thing (hand-edited label is honored), just asserts the label that survives grouping in the new correct position.

M6: `notebook_report/review.py` adds `write_review(cells, path)` and `read_review(path) -> list[Cell]`, using PyYAML (already a dependency, no new install). `write_review` serializes every `Cell` field the render path needs — including `outputs`, round-tripped through `json.loads(json.dumps(cell.outputs))` first because nbformat's `outputs` are `NotebookNode` (a dict subclass) that PyYAML's `safe_dump` can't represent directly, while `json` handles dict subclasses natively (same trick already implicit in `test_render.py`'s `json.dumps(nb)` on a parsed notebook) — into a `{"cells": [...]}` YAML doc, one key per line, human-legible for hand-editing. `read_review` rebuilds `Cell` objects and validates every entry's `label` against `classifier.ALL_LABELS` (`SETUP`/`EXPLORATION`/`RESULT`/`ABANDONED`/`UNCLASSIFIED`) before any field is read, raising `ValueError` naming the offending cell id and label on a violation — the CONTEXT.md trust-boundary #3 requirement ("render validates every entry against the same label enum ... a violation fails render with a clear message") — except `label: None` (a cell that was never run through the classifier) is accepted as "not yet classified," distinct from a garbage string; see Decisions. `classifier.py` gained `ALL_LABELS = LABELS + ("UNCLASSIFIED",)` so `review.py` validates against the same enum the classifier actually produces (including its own fallback value) rather than duplicating the list. `cli.py`'s `analyze` is implemented for the first time (parse → annotate_staleness → classify_cells → write_review to `review.yaml` in cwd) and `render`'s argument changed from `notebook_path` back to `review_path` (reads `review.yaml`, no longer touches the notebook or the stale-hash cache directly) — the rename flagged as expected back at M2/M4. `templates/report.html.jinja` gained a `<span class="label">{{ cell.proposed_label }}</span>` (only emitted when a label is set) so a hand-edited label is visible in the rendered output — this is plumbing to make M6's own acceptance test observable, not the section-grouping that PLAN.md reserves for Module: Locked Sections + Polish (M7); no grouping/headers added here.

M4: `notebook_report/templates/report.html.jinja` adds `{% if cell.stale %}<div class="gap-box" data-cell-id="{{ cell.id }}">Gap: {{ cell.reason }}</div>{% endif %}` right after the output `<pre>`, so a stale cell's gap box sits next to its (possibly outdated) output. `cli.py`'s `render` command now calls `annotate_staleness(cells, cache_path)` between `parse_notebook` and `render_report` — cache path is `Path(f"{Path(notebook_path).stem}.cache.json")`, i.e. a filename-only relative path resolved against cwd (same place `report.html` is written), not next to the source notebook — this matters because the M2 acceptance test invokes `render` with an absolute path into `tests/fixtures/`, and a notebook-directory-relative cache path would have written a stray `.cache.json` into the committed fixtures directory on every test run. `.gitignore` gained `/*.cache.json` for the root-level case, matching the existing `/report.html` and `/review.yaml` stray-file entries.

M5: `notebook_report/classifier.py` adds `classify_cells(cells, client=None) -> list[Cell]` and `classify_cell(cell, client) -> Cell`. Uses Anthropic tool-forced structured output (`tool_choice={"type": "tool", "name": "classify_cell"}`) with a hand-written JSON tool schema (label enum + gloss string), not `model_json_schema()` — kept the schema and the `CellClassification` Pydantic model as two small hand-authored pieces rather than deriving one from the other, simplest thing that passes acceptance. Every model response is validated via `CellClassification.model_validate(tool_use.input)` before any field is read (never branches on unvalidated JSON, per CLAUDE.md); on `ValidationError` or a malformed/missing tool-use block, retries once (`MAX_ATTEMPTS = 2` total attempts), then falls back to `proposed_label="UNCLASSIFIED"`, `proposed_gloss=None`. `Cell` gained `proposed_label: str | None = None` and `proposed_gloss: str | None = None` (defaulted, so M1-M4 construction sites and tests are untouched). Reuses `render.py`'s `_output_text` to build the cell's prompt context rather than duplicating output-extraction logic. `client` is an injected dependency (defaults to `anthropic.Anthropic()`, which reads `ANTHROPIC_API_KEY` from the environment itself — classifier.py never opens or parses `.env`), so the acceptance test passes a fake client with canned tool-use responses instead of hitting the network. No new dependency: `anthropic` and `pydantic` were already in `pyproject.toml`/`requirements.txt` from Module 0. `cli.py`'s `analyze` command is untouched (still `NotImplementedError`) — wiring the classifier into the CLI is Module: Review Artifact (M6), not this module.

M1: `notebook_report/parser.py` defines a `Cell` dataclass (id, source, cell_type, outputs, execution_count, stale, reason) and `parse_notebook(path) -> list[Cell]`, which reads via `nbformat.read(path, as_version=4)`, strips cells whose source is empty/whitespace-only, and sorts by `(execution_count is None, execution_count, original_index)` — executed cells ordered by execution_count, unexecuted/markdown cells pushed to the end in original relative order (not itself acceptance-tested; see Decisions).

M2: `notebook_report/render.py` adds `render_report(cells) -> str`, a Jinja2 `Environment` (autoescape on) loading `templates/report.html.jinja`, with a custom `output_text` filter that extracts display text from a cell's `outputs` list (stream text, `data["text/plain"]`, or error traceback, in that priority order). The template emits one `<section class="cell">` per cell with a `<pre class="source">` and, only if output text is non-empty, a `<pre class="output">`. `cli.py`'s `render` command now works end-to-end: parses the notebook, renders, writes `report.html` to cwd. `analyze` is still a stub. The `render` command's Click argument was renamed from `review_path` to `notebook_path` — it takes the `.ipynb` directly for now since Module: Review Artifact (M6, which introduces `review.yaml` and the analyze/render split) hasn't been built yet; expect this argument to change again at M6.

M3: `notebook_report/staleness.py` adds `annotate_staleness(cells, cache_path) -> list[Cell]`. Cache is a flat JSON dict `{cell_id: sha256(normalized_source)}` at a caller-supplied path, loaded if present, rewritten (all current hashes) on every call. Per cell: no prior hash → not stale (first-sight baseline); hash differs from cached → `stale=True`, `reason` set; hash matches → not stale. `Cell` gained `stale: bool = False` and `reason: str | None = None` fields (defaulted, so M1's Cell construction sites and test are untouched). Resolves the CONTEXT.md open question: source is normalized (`re.sub(r"\s+", " ", source).strip()`) before hashing, so whitespace-only edits are never flagged stale — see Decisions.

Verified directly against the repo this pass:
- `git log -1 --oneline` → `9228c41 Module 0 + M1 + M2: bootstrap, notebook parser, bare HTML renderer`
- `.venv/bin/python -m pytest -q` → 7 passed (adds `test_classifier.py::test_classify_cells_labels_unambiguous_cells_and_falls_back_on_malformed_response`)
- No live API call made this pass — the acceptance test runs entirely against a fake client (see M5 notes); the account's credit status (see Gotchas) is still unverified as fixed.

## Next step
1. M6 (Review Artifact) and M7 (Locked Sections + Polish) are built and passing, neither committed. Human should review and commit both — M7 is PLAN.md's final module, so once committed, PLAN.md's MVP scope is complete end to end.
2. No further modules queued; PLAN.md has no Module 8. Any follow-on work (README polish, real fixtures beyond the current synthetic ones, the resume bullets' [N]/[X]/[Z] placeholders) is outside a specific module and should wait for explicit direction.
3. The Anthropic account still needs credits topped up (see Gotchas) before `analyze` can be exercised live end-to-end against the real API — M6/M7's acceptance tests all used a monkeypatched `classify_cells` or bypassed classification entirely, same no-network pattern as M5, so this remains a soft blocker only, not a release blocker for the code itself.

## Architecture
Stack: Python 3.11, nbformat, Anthropic SDK, Pydantic, Click, Jinja2, PyYAML; pytest for tests.
Files:
- `notebook_report/__init__.py` — empty package marker.
- `notebook_report/cli.py` — Click group; `analyze` implemented (M6: parse → annotate_staleness → classify_cells → write review.yaml to cwd; M7 wraps `parse_notebook` in try/except for malformed input, raising `click.ClickException`); `render` implemented (M2 + M4 + M6: reads `review_path`, i.e. `review.yaml`, no longer touches the notebook or cache directly). Entrypoint registered via pyproject.toml `[project.scripts]`.
- `notebook_report/parser.py` — implemented (M1): `Cell` dataclass + `parse_notebook`.
- `notebook_report/render.py` — implemented (M2, extended M7): `render_report(cells)` + `_output_text` Jinja filter; M7 adds `SECTION_ORDER`, `SECTION_TITLES`, `_group_cells(cells)` grouping cells by `proposed_label` into canonically-ordered, non-empty section buckets before handing them to the template.
- `notebook_report/templates/report.html.jinja` — implemented (M2 + M4 + M6 + M7): cells now render inside `<section class="label-group" data-label="...">` blocks (one per non-empty canonical section, M7), each still containing the per-cell `<section class="cell">` with a `<span class="label">` when `proposed_label` is set, source + optional output `<pre>`, plus a `<div class="gap-box">` when `cell.stale` is true.
- `notebook_report/staleness.py` — implemented (M3): `annotate_staleness(cells, cache_path)`, JSON hash-cache sidecar, whitespace-normalized source hashing.
- `notebook_report/classifier.py` — implemented (M5, extended M6): `classify_cells(cells, client=None)` / `classify_cell(cell, client)`, tool-forced structured output, Pydantic-validated, retry-then-`UNCLASSIFIED` fallback; `ALL_LABELS = LABELS + ("UNCLASSIFIED",)` added M6 for `review.py`'s validation. Wired into `cli.py`'s `analyze` as of M6.
- `notebook_report/review.py` — implemented (M6): `write_review(cells, path)` (Cell list → human-editable `review.yaml`) and `read_review(path) -> list[Cell]` (validates every `label` against `classifier.ALL_LABELS`, `None` excepted, before use; raises `ValueError` naming the bad cell/label on violation).
- `tests/test_smoke.py` — imports the package. `tests/test_parser.py` — M1 acceptance test. `tests/test_render.py` — M2 + M4 + M7 acceptance tests (routed through `write_review`/`render review.yaml` per the M6 CLI contract change; M7 adds `test_render_groups_cells_into_label_sections_in_canonical_order`, asserting `<section class="label-group">` order follows `SECTION_ORDER` rather than document order). `tests/test_staleness.py` — M3 acceptance tests. `tests/test_classifier.py` — M5 acceptance test (fake-client, no network). `tests/test_review.py` — M6 + M7 acceptance tests: `analyze` (classifier monkeypatched, no network) writes review.yaml, hand-editing a label and re-rendering reflects the edit in the `<span class="label">` (M7: assertion order updated to canonical-section order, see Decisions); a review.yaml with an out-of-enum label fails `render` with a clear error; M7 adds `test_analyze_on_corrupt_notebook_fails_cleanly` (non-JSON `.ipynb` → clean `ClickException`, no traceback). `tests/fixtures/out_of_order.ipynb` — out-of-order execution_counts + one whitespace-only cell (M1/M2). `tests/fixtures/staleness_base.ipynb` — two stably-id'd cells (`cell-a`, `cell-b`) used as a base that tests mutate between "runs" (M3, M4). `tests/fixtures/corrupt.ipynb` — deliberately non-JSON content, M7's malformed-input fixture.
- `pyproject.toml`, `requirements.txt` (pip freeze, 35 lines), `.gitignore` (now also ignores root-level `*.cache.json`), `.env.example` — all new from module 0.
- `.env` — created at module 0, gitignored, key value not yet confirmed working (see Gotchas).
Flow: `.ipynb` → parser → staleness → classifier (LLM, advisory) → review.yaml (human-edited) → render (grouped by label) → report.html. This is the actual CLI flow end to end (`analyze` then `render`) — parser (M1), bare render (M2), staleness (M3), gap boxes (M4), classifier (M5), review artifact + CLI split (M6), and locked sections + clean malformed-input errors (M7) are all implemented. PLAN.md's full module list (Slices 1-4) is now built; both M6 and M7 are pending human review/commit.

## Decisions (append-only)
- [2026-07-24] Used uv's cached CPython 3.11.11 (`~/.local/share/uv/python/cpython-3.11.11-linux-x86_64-gnu/bin/python3.11`) to create `.venv`, instead of the system `python3` (3.10.12, which doesn't meet CLAUDE.md's 3.11+ requirement). Reason: uv already had 3.11.11 installed locally from prior use; no system/apt package install needed, so this doesn't trip the "new dependency needs approval" rule. SETTLED.
- [2026-07-24] `parse_notebook` sorts cells without an `execution_count` (markdown cells, or code cells never run) to the end of the list, in their original relative order, rather than interleaving them by position. Reason: PLAN.md's acceptance test only specifies ordering for executed code cells; this is the simplest rule that doesn't require guessing where an unexecuted cell "belongs" relative to executed ones. Not acceptance-tested (fixture is all-code-cells) — revisit if a later module's fixture shows this is wrong for markdown-heavy notebooks. SETTLED for M1.
- [2026-07-24] "Truly empty" cell = `source.strip() == ""` (whitespace-only or zero-length), independent of `execution_count`. Reason: matches PLAN.md's fixture description ("one truly empty cell") and CONTEXT.md's open question about whitespace-only edits not counting as stale (M3 concern) — kept the empty-cell-stripping rule in M1 orthogonal to that. SETTLED for M1.
- [2026-07-24] M2's `render` CLI command takes the notebook path directly (`notebook-report render fixture.ipynb`), not a `review.yaml` — the Click argument was renamed from the module-0 stub's `review_path` to `notebook_path`. Reason: PLAN.md's own acceptance test for this module calls it that way; the review.yaml split is a later module (M6, Review Artifact). Expect another rename at M6. SETTLED for M2.
- [2026-07-24] Output text extraction priority in `render.py`'s `_output_text`: stream `text` first, then `data["text/plain"]` (execute_result/display_data), then `traceback` (error), first match wins per output entry, joined with newlines across multiple outputs. Reason: simplest deterministic precedence that covers the three common nbformat output_types without needing to branch on `output_type` explicitly. Not stress-tested against rich outputs (images, HTML tables) — those fall through to nothing shown, acceptable for the bare renderer. SETTLED for M2.
- [2026-07-24] Staleness hashing normalizes source before hashing (`re.sub(r"\s+", " ", source).strip()`, then sha256) rather than hashing raw source. Reason: resolves the open question named in CONTEXT.md Risks — a purely cosmetic/whitespace edit (reformatting, reindent) does not invalidate a cell's recorded output, and false-positive staleness flags would erode trust in the gap-box mechanism the same way an autonomous mis-classification would. Tradeoff accepted: a semantically-meaningful edit that happens to only touch whitespace (rare) would be missed — acceptable, matches PLAN.md's "simplest approach that passes acceptance." SETTLED for M3.
- [2026-07-24] Cache is keyed by cell `id` (from nbformat, stable across edits to the same cell) and stores one hash per id, fully rewritten on every `annotate_staleness` call. A cell id with no prior cache entry is treated as not-stale (first-sight baseline) rather than stale-by-default. Reason: matches PLAN.md's acceptance test ("first run creates the cache with zero stale cells") and means a newly-added cell never spuriously flags as stale. SETTLED for M3.
- [2026-07-24] M4 built directly on top of uncommitted M3, per explicit user instruction ("start module 4") rather than waiting for a human commit first. Reason: repeats the same pattern already used for M2 on uncommitted M1 last session — read as the user's actual working cadence (build several modules, review/commit in one batch) rather than a one-off exception. Flagged in the report packet each time rather than silently treated as the new default; still never proceeds past an explicit "start module N" instruction on my own initiative. SETTLED as observed pattern, not changed as a rule in CLAUDE.md.
- [2026-07-24] Stale-hash cache path for the CLI's `render` command is `Path(f"{Path(notebook_path).stem}.cache.json")` — resolved against cwd, not against the notebook's own directory. Reason: the M2 acceptance test invokes `render` with an absolute path into the committed `tests/fixtures/` directory; a notebook-directory-relative cache path would write a stray `.cache.json` into that directory on every test run. Resolving against cwd instead matches where `report.html` already gets written and keeps test runs from mutating tracked directories. Tradeoff: two different notebooks sharing a stem, rendered from the same cwd, would collide on one cache file — acceptable for v1 single-notebook-at-a-time CLI usage. SETTLED for M4.
- [2026-07-24] M5's Anthropic client is an injected parameter (`classify_cells(cells, client=None)`), defaulting to `anthropic.Anthropic()` only when not supplied. Reason: the acceptance test can't hit the real API (no credits — see Gotchas) and CLAUDE.md requires the M5 acceptance test to run "against a mocked/fixed model response"; a fake object satisfying `.messages.create(**kwargs) -> object with .content` is enough to drive the real validation/retry/fallback code path with zero network dependency. SETTLED for M5.
- [2026-07-24] Classification uses Anthropic tool-forced structured output (`tool_choice` pinned to the one tool) with a hand-written JSON schema in `CLASSIFY_TOOL`, kept separate from the `CellClassification` Pydantic model rather than generating one from the other via `model_json_schema()`. Reason: PLAN.md's stack section calls for "Pydantic tool-forced structured output," and two small hand-written pieces are simpler to read and test than schema-generation machinery for a 2-field model. Every response is validated through `CellClassification.model_validate(...)` regardless of the tool schema, so the trust boundary (never branch on unvalidated model JSON) doesn't depend on the tool schema being airtight. SETTLED for M5.
- [2026-07-24] Retry-then-fallback is capped at `MAX_ATTEMPTS = 2` (one real retry) and treats five exception types as "malformed, try again": `ValidationError` (schema violation), `StopIteration` (no tool_use block in the response), `AttributeError`/`TypeError`/`KeyError` (unexpected response shape, e.g. `response.content` not iterable or missing `.input`). Reason: matches PLAN.md's acceptance wording exactly ("one retry, then the safe fallback") and CLAUDE.md's "Never branch on unvalidated model JSON" — every one of those exceptions means the response could not be trusted enough to read a field from. SETTLED for M5.
- [2026-07-24] `review.py`'s `read_review` treats `label: None` as valid (meaning "not yet classified") distinct from an out-of-enum string, which is rejected. Reason: a cell that never went through `classify_cells` (e.g. a review.yaml assembled without calling `analyze`) legitimately has no label yet; only a garbage/typo'd label string is the trust-boundary violation CONTEXT.md's #3 boundary is guarding against. Also needed for M2/M4's pre-existing render tests, which write a review.yaml directly via `write_review` without running the classifier first. SETTLED for M6.
- [2026-07-24] `write_review` round-trips `cell.outputs` through `json.loads(json.dumps(...))` before handing it to `yaml.safe_dump`. Reason: nbformat's `outputs` are `NotebookNode`, a dict/list subclass PyYAML's safe representer doesn't know how to serialize (raises `RepresenterError`); `json` serializes dict/list subclasses structurally by `isinstance`, so round-tripping through it first yields plain `dict`/`list` that YAML can represent. SETTLED for M6.
- [2026-07-24] Added a bare `<span class="label">{{ cell.proposed_label }}</span>` to the template (no grouping, no headers) so M6's acceptance test ("render reflects the hand-edited label") has something concrete to assert on. Reason: PLAN.md explicitly reserves section-grouping-by-label for Module: Locked Sections + Polish (M7); this is the minimum plumbing to make the label's effect on render observable now without building M7 early. Revisit/replace when M7 groups cells into `<section>`s by label — this span may become redundant or move into a section header. SETTLED for M6, expected to change at M7.
- [2026-07-24] M7 built directly on top of uncommitted M6, per explicit user instruction ("start module 7"). Reason: same observed cadence as M4-on-M3 (see above) — build several modules, review/commit in one batch. Flagged here and in the report packet rather than treated as a silent new default. SETTLED as observed pattern, not changed as a rule in CLAUDE.md.
- [2026-07-24] A cell whose `proposed_label` is `None` (never classified) or the literal `"UNCLASSIFIED"` string (classifier fallback after exhausting retries) both land in the same `UNCLASSIFIED` section bucket in `_group_cells`. Reason: from a report reader's perspective these are the same thing — "no reliable label" — and PLAN.md's four named sections don't cover either case; a fifth catch-all bucket is the simplest thing that doesn't silently drop cells or crash on an unexpected label. Empty buckets are omitted from the section list entirely, so a fully-classified notebook shows no "Unclassified" section at all. SETTLED for M7.
- [2026-07-24] Section-header markup keeps the existing per-cell `<span class="label">` inside each cell (didn't remove it despite M6's Decisions note flagging it as possibly redundant once grouping landed). Reason: M6's own acceptance test (`test_analyze_writes_review_yaml_and_render_reflects_hand_edited_label`) asserts on that span directly; PLAN.md's M7 scope is "group into sections," not "redesign per-cell label display" — removing it would be scope creep and would require rewriting a passing M6 test for no acceptance-driven reason. SETTLED for M7, revisit only if a future module asks for a leaner per-cell markup.
- [2026-07-24] Grouping cells by canonical section order broke M6's already-passing `test_analyze_writes_review_yaml_and_render_reflects_hand_edited_label` assertion (`labels == ["RESULT", "EXPLORATION"]`, which assumed document order). Updated the expected order to `["EXPLORATION", "RESULT"]` (canonical `SECTION_ORDER` order) with an inline comment explaining why, rather than leaving the test broken or avoiding the section reorder to keep the old assertion passing. Reason: PLAN.md's M7 acceptance test explicitly requires grouped sections; document-order rendering and grouped rendering are mutually exclusive, and grouping is the actual module deliverable. Flagged as a deviation (an already-passing test's assertion changed) in the report packet. SETTLED for M7.
- [2026-07-24] `cli.py`'s `analyze` catches exactly `nbformat.reader.NotJSONError` and `nbformat.ValidationError` (confirmed via REPL to be the two exceptions nbformat actually raises — the former on non-JSON content, the latter, which is `jsonschema.exceptions.ValidationError` re-exported, on JSON that fails the notebook schema) and converts to `click.ClickException`. Reason: CONTEXT.md trust boundary #1 asks for "a clean CLI error (Module 7), never a raw stack trace" specifically for malformed `.ipynb` input; catching only these two (not a bare `except Exception`) keeps the boundary narrow — a genuinely unexpected error (e.g. a bug in `parse_notebook` itself) still surfaces as a real traceback rather than being masked as a benign "bad notebook" message. SETTLED for M7.

## Constraints and hard rules
- No auto-commit, ever — human commits Module 0 (and every module) by hand.
- `.env` is never read, written-to-content, or echoed by the agent; only loaded by a script via env vars for the smoke call.
- Never module N+1 before module N's acceptance passes and is committed.
- No external deadline (normal mode). Local CLI only, no hosting.

## Gotchas (append-only)
- [2026-07-24] System `python3` is 3.10.12, but CLAUDE.md requires 3.11+; no python3.11 was installed system-wide (apt candidate existed but wasn't installed). Instead: create `.venv` with the uv-cached 3.11.11 interpreter directly (see Decisions above), don't apt-install a new system Python.
- [2026-07-24] Module 0 step 8 (live API smoke call) got blocked twice: first, no `ANTHROPIC_API_KEY` was set in the shell env or in any `.env` file (none existed yet); second, once asked, the user reported their existing key's credits had expired. Confirmed: the smoke call (`claude-haiku-4-5-20251001`, `max_tokens=1`) returned `400 invalid_request_error: Your credit balance is too low` — not a `401 authentication_error`, so the key itself is valid and the SDK/env-loading path works; the account just has no credits. Instead: treat this as "key mechanism confirmed, account needs credits," not a broken integration — re-run the same smoke call once credits are topped up to get an actual successful response.

## Open questions
- None open. (The whitespace/formatting-only-edit question from CONTEXT.md Risks is resolved — see Decisions, 2026-07-24, "Staleness hashing normalizes source before hashing.")

## Run and verify
```
.venv/bin/python -m pytest -q          # 11 passed
.venv/bin/notebook-report --help       # lists analyze, render
.venv/bin/notebook-report analyze tests/fixtures/out_of_order.ipynb   # writes review.yaml + out_of_order.cache.json to cwd (needs a funded ANTHROPIC_API_KEY, see Gotchas)
.venv/bin/notebook-report analyze tests/fixtures/corrupt.ipynb        # clean "Could not parse notebook ..." error, exit 1, no traceback
.venv/bin/notebook-report render review.yaml                          # writes report.html to cwd, grouped into label-group sections
git log -1 --oneline                   # d21f9f5 (M3 + M4 + M5); M6 + M7 changes pending on top, not yet committed
git status --short                     # M6 + M7 changes pending, not yet committed (see Files changed in latest report packet)
```
